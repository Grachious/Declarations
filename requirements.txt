# Обязательные зависимости (решение работает без интернета и без GPU только на них)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1

# Опционально: улучшает лемматизацию русских слов (полностью офлайн,
# словари поставляются внутри пакета). Если не установлен — run.py
# автоматически переключается на встроенный суффиксный стеммер.
pymorphy2==0.9.1
pymorphy2-dicts-ru==2.4.417127.4579844

# Опционально: включает семантический (embedding) сигнал. Требуется
# только если вы заранее (с интернетом) запустили download_model.py.
# sentence-transformers==3.0.1
# torch==2.3.1
